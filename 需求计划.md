# Markdown Finder 快速预览渲染工具需求计划

## 1. 项目目标

建立一个主要服务于 Markdown 文档的 macOS 快速预览工具，让用户在 Finder 中选中 `.md` 文件后，可以通过空格键或预览面板看到渲染后的文档内容，而不是系统默认显示的 Markdown 源码。

第一版的核心价值是：让 Markdown 文档在 Finder 里可以被快速阅读、识别和筛选。

## 2. 第一版范围

- 面向本机自用和快速迭代。
- 优先支持当前主力系统 macOS 26.x。
- 以 Finder Quick Look 预览扩展作为核心能力。
- 先完成需求文档和技术方向确认，不立即初始化复杂工程。
- 后续实现阶段再创建 Xcode/macOS App 与 Quick Look Extension 工程文件。

## 3. 非目标

第一版不做以下能力：

- 不做完整 Markdown 编辑器。
- 不做文档库管理、标签管理或全文搜索。
- 不做公开安装包、DMG、签名、公证或 App Store 上架。
- 不追求完整兼容所有 Markdown 方言和高级插件生态。
- 不在第一版内实现 Mermaid、数学公式、脚注等复杂扩展。

## 4. 产品形态

工具采用轻量 macOS 容器 App + Quick Look Preview Extension 的形态。

容器 App 的职责保持克制：

- 承载和安装 Quick Look 扩展。
- 提供简单说明，让用户知道该工具用于启用 Finder Markdown 预览。
- 必要时提供基础状态或帮助信息。

核心预览体验发生在 Finder 中：

- 用户选中 Markdown 文件。
- 按空格键打开 Quick Look。
- Quick Look 展示渲染后的 Markdown 文档。

## 5. 核心能力

第一版应覆盖常用 Markdown 文档阅读场景：

- 标题。
- 段落。
- 加粗、斜体等基础强调。
- 有序列表和无序列表。
- 引用块。
- 表格。
- 任务列表。
- 链接。
- 围栏代码块。
- 代码高亮。
- 本地相对路径图片。

本地图片处理规则：

- 相对路径图片应基于 Markdown 文件所在目录解析。
- 图片缺失时显示轻量占位或错误提示。
- 图片缺失不应导致整个文档预览失败。

链接处理规则：

- 链接在视觉上应被渲染为链接样式。
- 第一版不需要实现复杂的文档间跳转或预览内导航。

## 6. 视觉风格

预览样式采用清爽阅读风，重点服务快速扫读和文档识别。

视觉目标：

- 排版干净，层级清晰。
- 正文行宽舒适，不铺满过宽窗口。
- 标题、列表、引用、表格有明确视觉区分。
- 代码块具有较好的可读性和基础高亮。
- 浅色主题优先。
- 暗色模式可作为低成本增强，如果实现成本较低则支持。

整体观感应更接近优秀文档站的阅读体验，而不是系统原生纯文本预览。

## 7. 安全边界

Markdown 文件可能包含 HTML、远程资源或潜在脚本，因此第一版采用安全本地预览策略。

安全要求：

- 禁用脚本执行。
- 不主动加载远程资源。
- 允许加载本地相对路径图片。
- 对原始 HTML 进行过滤、限制或仅允许安全子集。
- 渲染失败时显示轻量错误预览，而不是执行不可信内容。

远程图片和远程资源默认不加载，以避免隐私泄露、网络依赖和安全风险。

## 8. 技术路线

第一版技术路线采用 HTML 预览：

- Quick Look Extension 接收 Finder 传入的 Markdown 文件。
- 扩展读取文件内容。
- Markdown 渲染器将内容转换为安全 HTML。
- 渲染结果附带内置 CSS 样式。
- Quick Look 通过 HTML 数据展示预览内容。

该路线的优点：

- 最贴合 Finder 快速预览场景。
- 实现复杂度适中。
- 阅读体验明显优于纯源码预览。
- 后续可以继续扩展主题、目录、更多 Markdown 语法能力。

暂不采用的路线：

- PDF 预览：版式稳定，但更像导出，不够适合快速滚动阅读。
- 自定义 WebView 预览：未来能力强，但第一版复杂度和调试成本更高。

## 9. 异常处理

第一版需要覆盖以下异常场景：

- 空 Markdown 文件：显示空状态或极简提示。
- 文件读取失败：显示无法读取文件的错误预览。
- 编码异常：尽量以 UTF-8 读取，失败时提示编码不受支持。
- Markdown 渲染失败：显示渲染失败提示，不让 Quick Look 崩溃。
- 本地图片缺失：显示图片缺失占位，不影响其他内容。
- 文件过大：必要时限制渲染大小，并提示文档过大。
- 恶意 HTML 或脚本：过滤或禁用，不执行脚本。
- 远程资源：默认不加载，并可显示为普通链接或占位。

## 10. 测试场景

实现阶段需要验证以下场景：

- Finder 中选中 `.md` 文件并按空格键，可以看到渲染后的文档。
- Finder 预览面板可以显示渲染后的 Markdown。
- 中文和英文混排显示正常。
- 标题、列表、引用、表格、任务列表渲染正常。
- 围栏代码块显示正常，并具备基础高亮。
- 本地相对路径图片可以显示。
- 图片缺失时不会导致预览失败。
- 空文件有合理显示。
- 大文件不会导致明显卡死或崩溃。
- 包含脚本的 Markdown 不会执行脚本。
- 远程图片或远程资源不会被主动加载。
- 文件修改后 Finder 预览刷新行为可接受。

## 11. 实施计划

第一阶段：

- 创建项目目录。
- 建立本需求文档。
- 确认产品范围和技术路线。

第二阶段：

- 设计 Swift/macOS 项目结构。
- 创建 macOS 容器 App。
- 创建 Quick Look Preview Extension。
- 接入 Markdown 到 HTML 的渲染流程。

第三阶段：

- 增加安全过滤和本地图片解析。
- 打磨内置 CSS 样式。
- 补充样例 Markdown 文件和测试场景。
- 在 Finder 中进行实际预览验证。

## 12. 当前假设

- 项目名称采用 `markdown-quicklook-preview`。
- 项目路径为 `/Users/sokei/SoKei/code/markdown-quicklook-preview`。
- 第一版只面向本机开发与使用。
- 第一版优先支持 macOS 26.x。
- 文档先作为需求基线，不立即创建代码工程骨架。

